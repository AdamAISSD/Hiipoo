# Hiipoo Admin App Placeholder

This directory is intentionally reserved for a future static or authenticated admin application.

The public website currently has no server and no payment flow. Product facts, localized copy, capacity recommendations, and compliance notes live in `packages/content` so a future admin app can edit or validate the same data model without rewriting the website.
